source 'https://rubygems.org'

# Specify your gem's dependencies in prism-rails.gemspec
gemspec

# Keep the gem's development bundle aligned with the latest Rails line.
# Older Rails compatibility should be exercised in a matrix, not by pinning the
# default local bundle to outdated transitive dependencies.
gem "railties", "~> 8.1"
