# prism-rails Rails Demo

This demo app shows `prism-rails` running in the asset pipeline on Rails 8.1.

```sh
cd demo/rails
bundle install
bin/rails server
```

Open http://localhost:3000 to see Ruby, JavaScript, and HTML snippets highlighted by Prism.
